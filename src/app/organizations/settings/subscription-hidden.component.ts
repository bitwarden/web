import { Component } from "@angular/core";

// Component is used so that the SVG can embed CSS color variables
@Component({
  selector: "app-org-subscription-hidden",
  templateUrl: "subscription-hidden.component.svg",
})
export class SubscriptionHiddenComponent {}
